# Usage

1. Start the daemon:

   ```bash
   go -C daemon run .
   ```

2. Copy the `connection token` printed by the daemon. Do not copy the daemon bearer token.
3. Open the UI, choose **Connect daemon**, paste the connection token, and submit.
4. Create and manage tasks through the connected daemon in the UI.
