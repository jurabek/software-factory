{
  "name": "software-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["web"],
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "npm --prefix web run build",
    "typecheck": "npm --prefix web run typecheck",
    "test": "go test ./...",
    "swagger:validate": "go run github.com/go-swagger/go-swagger/cmd/swagger@v0.36.5 validate swagger.yaml"
  }
}
